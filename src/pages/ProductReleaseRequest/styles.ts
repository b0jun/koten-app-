import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  first: {
    flex: 1,
  },
  second: {
    flex: 0.3,
    textAlign: 'center',
  },
  third: {
    width: 70,
    marginRight: 8,
    textAlign: 'center',
    alignItems: 'center',
  },
});

export default styles;
