import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  first: {
    flex: 1,
  },
  second: {
    flex: 1.8,
  },
  third: {
    width: 70,
    marginRight: 8,
    textAlign: 'center',
    alignItems: 'center',
  },
});

export default styles;
